name: Ghost Blog Management

on:
  workflow_dispatch:
    inputs:
      script:
        description: 'Script to run (e.g., restart_ghost.py, diagnose_ghost.py)'
        required: true
        default: 'update_ghost_core.py'
      action:
        description: 'Optional: Aktion für das Skript (z. B. system, status bei setup_server_backup.py)'
        required: false
        default: ''

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create .env file from Secrets
        run: |
          # This step creates the .env file that your scripts expect.
          # Scripts load it from scripts/.env (Path(__file__).parent / '.env'), not the repo root.
          # It maps GitHub Secrets to the .env file format.
          echo "VPS_IP=${{ secrets.VPS_IP }}" >> scripts/.env
          echo "VPS_USER=${{ secrets.VPS_USER }}" >> scripts/.env
          echo "VPS_PW=${{ secrets.VPS_PW }}" >> scripts/.env
          echo "BLOG_DOMAIN=${{ secrets.BLOG_DOMAIN }}" >> scripts/.env
          echo "BLOG_URL=${{ secrets.BLOG_URL }}" >> scripts/.env
          echo "ADMIN_EMAIL=${{ secrets.ADMIN_EMAIL }}" >> scripts/.env
          echo "GHOST_API_URL=${{ secrets.GHOST_API_URL }}" >> scripts/.env
          echo "GHOST_ADMIN_API_KEY=${{ secrets.GHOST_ADMIN_API_KEY }}" >> scripts/.env
          echo "GHOST_CONTENT_API_KEY=${{ secrets.GHOST_CONTENT_API_KEY }}" >> scripts/.env
          echo "GHOST_API_VERSION=${{ vars.GHOST_API_VERSION }}" >> scripts/.env

          # Database - MYSQL_DATABASE_NAME is required by scripts!
          echo "MySQL_database_name=${{ secrets.MYSQL_DATABASE_NAME }}" >> scripts/.env
          echo "MySQL_username=${{ secrets.MYSQL_USERNAME }}" >> scripts/.env
          echo "MySQL_password=${{ secrets.MYSQL_PASSWORD }}" >> scripts/.env
          echo "MySQL_hostname=${{ secrets.MYSQL_HOSTNAME }}" >> scripts/.env

      - name: Run Script
        env:
          # Nur als Umgebungsvariablen (maskiert), nicht in scripts/.env – JSON würde echo zerlegen.
          RCLONE_DRIVE_TOKEN: ${{ secrets.RCLONE_DRIVE_TOKEN }}
          RCLONE_CRYPT_PASS: ${{ secrets.RCLONE_CRYPT_PASS }}
          SERVER_BACKUP_ACTION: ${{ inputs.action }}
        run: |
          SCRIPT_NAME="${{ inputs.script }}"
          if [ -z "$SCRIPT_NAME" ]; then
            SCRIPT_NAME="restart_ghost.py"
          fi
          echo "Running scripts/$SCRIPT_NAME..."
          python "scripts/$SCRIPT_NAME"